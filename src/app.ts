// import "express-async-errors";
// import "reflect-metadata";
import express from "express";

const app = express();
app.use(express.json());

// app.use("/login");
// app.use("/users");
// app.use("/activities");

export default app;